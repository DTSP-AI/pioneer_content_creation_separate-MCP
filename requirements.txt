# Core Orchestration (REQUIRED)
langgraph==0.2.27
langchain==0.3.23
langchain-anthropic==0.3.6
langchain-community==0.3.14
langchain-core>=0.3.51  # Compatible with langchain 0.3.23

# Memory & Database
# mem0ai==0.1.17  # Temporarily disabled - incompatible with langchain 0.3.x
asyncpg==0.30.0
psycopg2-binary==2.9.10
sqlalchemy[asyncio]==2.0.36
alembic==1.14.0

# Qdrant Vector Store (for conversation history)
qdrant-client==1.12.1
langchain-qdrant==0.2.0
langchain-openai==0.2.14  # For embeddings + GPT models

# FastAPI Backend
fastapi==0.115.5
uvicorn[standard]==0.34.0
python-multipart==0.0.19
websockets==14.1

# API Clients
# anthropic and httpx versions managed by langchain packages
aiohttp==3.11.10

# Model Context Protocol (MCP) for PiAPI integration
mcp==1.1.2
sse-starlette==2.2.1  # Server-Sent Events for MCP communication

# AI Video Generation (PRIMARY)
# piapi-python==1.0.0  # Install when available, or use httpx directly

# Legacy API Clients (OPTIONAL FALLBACK)
# elevenlabs==1.8.0  # Only if PiAPI fallback needed

# Google Services (Sheets, YouTube)
google-auth==2.37.0
google-auth-oauthlib==1.2.1
google-auth-httplib2==0.2.0
google-api-python-client==2.157.0
gspread==6.1.4
oauth2client==4.1.3

# Video Processing
ffmpeg-python==0.2.0
pillow==11.0.0

# Utilities
pydantic==2.10.3
pydantic-settings==2.7.0
python-dotenv==1.0.1
tenacity==9.0.0
python-dateutil==2.9.0

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
pytest-mock==3.14.0
pytest-httpx==0.30.0  # Compatible with Python 3.12

# Development
black==24.10.0
ruff==0.8.4
mypy==1.14.0
